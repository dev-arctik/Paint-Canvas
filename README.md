# Paint-Canvas
All 3 files use pygame to paint the canvas.
8a paints the canvas using squares with random color.
8b paints the canvas using dots with random color when you right click on canvas and drag.
8c creates strip of rgb.
